created api project with .net core 
used entity framework for the database communication
add the xunit test case and tested and observe all tets are succeeded
implemented repository pattern and registered in program.cs
add the config settings for cors polacy
created the react project and implemented to consume the webapi and designed the form 
done the unit test and observed its working fine
